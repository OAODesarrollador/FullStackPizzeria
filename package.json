{
  
    "scripts": {
      "start": "cd backend && npm run dev",
      "build": "cd backend && npm install && npm run build"
    },
  
  "dependencies": {
    "emailjs-com": "^3.2.0",
    "react-icons": "^5.3.0"
  }
 
}
